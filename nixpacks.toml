[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = [
  "BASE_PATH=/ NODE_ENV=production pnpm --filter @workspace/chat-miya run build",
  "pnpm --filter @workspace/api-server run build"
]

[start]
cmd = "NODE_ENV=production node artifacts/api-server/dist/index.mjs"
